{
  "name": "recursive-deps",
  "version": "1.1.1",
  "description": "finds all the required dependencies by walking the dependency tree starting from a specified array of source paths",
  "main": "index.js",
  "bin": {
    "recursive-deps": "./bin/cmd.js"
  },
  "scripts": {
    "lint": "jshint --config .jshintrc index.js bin/cmd.js",
    "test": "blue-tape test/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/allain/recursive-deps.git"
  },
  "keywords": [
    "require",
    "recursive",
    "dependencies"
  ],
  "author": "Allain Lalonde <allain.lalonde@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/allain/recursive-deps/issues"
  },
  "homepage": "https://github.com/allain/recursive-deps#readme",
  "dependencies": {
    "fj-flatten": "^1.0.0",
    "konan": "^1.2.1"
  },
  "devDependencies": {
    "blue-tape": "^1.0.0"
  }
}
